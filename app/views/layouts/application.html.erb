<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% unless session[:user_id] %>
        <li class="nav-item">
          <%= link_to 'Register', register_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Login', login_path, class: 'nav-link' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= button_to 'Logout', logout_path, method: :post, class: 'nav-link border-0 bg-transparent' %>
        </li>
      <% end %>

      <% if session[:user_id] %>

      <li class="nav-item">
      <%= link_to 'Posts', posts_url , class: 'nav-link border-0 bg-transparent' %>

      <li class="nav-item">
      <%= link_to 'New', new_post_url, class: 'nav-link border-0 bg-transparent' %>
    </li>
      <% end %>


        </ul>


    </div>
  </div>
</nav>

  <div class="container">
    <%= yield %>
  </div>

  </body>
</html>
